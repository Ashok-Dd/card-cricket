node_modules
/dist
# Keep environment variables out of version control
.env

/src/generated/prisma

# TypeScript incremental-build cache — a local build artifact, not source;
# committing it risks a stale cache masking real compile errors elsewhere.
*.tsbuildinfo
